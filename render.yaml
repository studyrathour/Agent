services:
  - type: web
    name: jules-whatsapp-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JULES_API_KEY
        sync: false
      - key: WHATSAPP_NUMBER
        sync: false
